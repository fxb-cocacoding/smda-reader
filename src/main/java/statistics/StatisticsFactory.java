package statistics;

public class StatisticsFactory {
	/*
	 * ToDo: Build a wrapper to read through all statistics.* packets, in this case only statistics.instructions
	 */
	public static void create() {
		StatisticChecker.main(null);
	}
}
